name: ci

on:
  push:
    branches:
      - 'master'
  pull_request:
    branches:
      - 'master'

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v6

        - uses: QEDK/soteria@v0.1.7
          with:
            directory: src/
